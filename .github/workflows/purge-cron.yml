name: purge-cron

on:
  schedule:
    - cron: "0 3 * * *"   # diário 03:00 UTC
  workflow_dispatch:

jobs:
  purge:
    runs-on: ubuntu-latest
    steps:
      - name: No-op purge daily
        run: echo "Executando limpeza diária (configure aqui seus scripts)."
