// This file defines the TypeScript types for your Supabase database. Defining
// the schema here allows the Supabase client helpers to provide useful
// type hints when interacting with your data. You can expand these types to
// reflect the real shape of your database tables and RPC functions. For now
// it uses `any` to keep things simple.

export type Database = any;